spring:
  application:
    name: zuul
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://mmall-eureka:8000/eureka/
  instance:
    prefer-ip-address: true
